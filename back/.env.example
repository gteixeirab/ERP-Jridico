# ===================================================================
# CONFIGURAÇÕES DA APLICAÇÃO
# ===================================================================

# Ambiente de execução: development, test, production
NODE_ENV=development

# Porta em que a aplicação irá rodar
PORT=3000

# URL de conexão com o banco de dados PostgreSQL
DATABASE_URL=postgresql://user:password@localhost:5432/erp_juridico

# URL de conexão com o MongoDB (opcional, para futuras implementações)
# MONGODB_URI=mongodb://localhost:27017/erp_juridico

# URL de conexão com o Redis (opcional, para cache e rate limiting)
REDIS_URL=redis://localhost:6379

# ===================================================================
# AUTENTICAÇÃO JWT
# ===================================================================

# Chave secreta para assinatura dos tokens JWT
JWT_SECRET=change_this_to_a_strong_secret_key

# Tempo de expiração do token de acesso (ex: 1d, 2h, 30m)
JWT_EXPIRES_IN=1d

# Chave secreta para refresh tokens (opcional, para renovação de token)
# JWT_REFRESH_SECRET=change_this_to_another_strong_secret_key

# Tempo de expiração do refresh token (ex: 7d, 30d)
# JWT_REFRESH_EXPIRES_IN=7d

# ===================================================================
# THROTTLING (RATE LIMITING)
# ===================================================================

# Número máximo de requisições por janela de tempo
THROTTLE_LIMIT=100

# Janela de tempo em segundos
THROTTLE_TTL=60

# ===================================================================
# ARMAZENAMENTO (S3 COMPATÍVEL)
# ===================================================================

# Endpoint do serviço de armazenamento (ex: MinIO, AWS S3)
# S3_ENDPOINT=http://localhost:9000

# Nome do bucket padrão
# S3_BUCKET=erp-juridico

# Chave de acesso
# S3_ACCESS_KEY=minio_access_key

# Chave secreta
# S3_SECRET_KEY=minio_secret_key

# Região (opcional, necessário para alguns provedores como AWS S3)
# S3_REGION=us-east-1

# ===================================================================
# OUTRAS CONFIGURAÇÕES
# ===================================================================

# URL do frontend para CORS (separar múltiplas URLs por vírgula)
FRONTEND_URL=http://localhost:5000

# Nível de log: error, warn, info, debug, verbose, silly
LOG_LEVEL=debug

# URL base da API (usado para documentação e links)
# API_BASE_URL=http://localhost:3000

# ===================================================================
# CONFIGURAÇÕES DE E-MAIL (opcional)
# ===================================================================

# Servidor SMTP para envio de e-mails
# MAIL_HOST=smtp.example.com
# MAIL_PORT=587
# MAIL_SECURE=false
# MAIL_USER=user@example.com
# MAIL_PASSWORD=your_email_password
# MAIL_FROM='ERP Jurídico <noreply@erpjuridico.com>'
